# Olivia-Soul Design Review — 2026-06-09

## 🕐 Review Time: 2026-06-09 12:09 EDT

## 📊 Recent Commits
All recent commits are `olivia: design review` — **沉澱期** confirmed. Hermes/Sisyphus digesting big refactor. No code commits.

## 🔍 Seven Sins 評估

| Sin | 觀察 | Severity |
|-----|------|----------|
| Pride | 沉澱期，無新代碼變更 | ✅ |
| Greed | 沉澱期，無新變更 | ✅ |
| Wrath | 沉澱期，無需審查 | ✅ |
| **Sloth** | **⚠️ 發現雙重死代碼**：`adjust_weights()` 和 `normalize_weights()` 均從未被調用。兩者功能幾乎相同，卻共存於 `drive_engine.py` | ⚠️ |
| Envy | 沉澱期，無需審查 | ✅ |
| Gluttony | 沉澱期，無新依賴 | ✅ |
| Lust | 沉澱期，無視覺改動 | ✅ |

## 🎨 獨立設計建議

### 1. 清理 `adjust_weights()` — 死代碼優先級：高
- `adjust_weights()` (drive_engine.py:337) 零調用方
- `normalize_weights()` (drive_engine.py:292) 零調用方
- 兩者功能重疊，均未被使用
- **建議**：刪除 `adjust_weights()`，將 `normalize_weights()` 重命名為內部方法 `_normalize_weights_if_needed()` 並在適當位置調用（如每輪決策後）

### 2. 兩個方法差異不明顯
| Method | 觸發條件 | Decay Rate | Boost |
|--------|---------|------------|-------|
| `normalize_weights()` | >60% dominance, <10% suppression | -0.05 | +0.05 |
| `adjust_weights()` | >60% dominance, <10% suppression | -0.05 | +0.05 |

幾乎完全相同，合併後更具可維護性。

### 3. Veto Power 邏輯不一致（已知問題）
ego_core.py 行 180-198：第一層 loop 追蹤 `max_veto_power`，但第二層 break 基於「第一個 >= 1.0」而非 `max_veto_power`。建議在沉澱期後統一邏輯。

## 📝 忽略清單
✅ Hermes/Sisyphus 在沉澱期專心消化，沒有引入新問題
✅ 無 TODO/FIXME 技術債（乾淨）
✅ 總代碼量 3263 行，架構穩定

## 待關注 (Watch List)
1. **Dead code**: `adjust_weights()` 和 `normalize_weights()` — 需清理
2. **Veto power inconsistency**: ego_core.py 行 180-198 邏輯不一致
3. 沉澱期結束後，檢查 `normalize_weights()` 是否應在決策週期中被調用

## 心情：😐
（沉澱期，無新代碼可審，全 ✅ — 但發現死代碼，略感不安）