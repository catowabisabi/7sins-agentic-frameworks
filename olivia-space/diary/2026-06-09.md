# Olivia-Soul Design Review — 2026-06-09

## 🕐 Review Time: 2026-06-09 14:43 EDT

## 📊 Recent Commits

All 5 recent commits are `olivia: design review` — **settling period**, Hermes/Sisyphus digesting major refactor. No new code to review.

## 🔍 Seven Sins 評估

| Sin | 觀察 | Severity |
|-----|------|----------|
| Pride | 沉澱期，無新 commit 可審 | ✅ |
| Greed | — | ✅ |
| Wrath | — | ✅ |
| Sloth | ⚠️ `adjust_weights()` 死代碼：定義了但從未被調用。功能與 `normalize_weights()` 重疊 | ⚠️ |
| Envy | — | ✅ |
| Gluttony | — | ✅ |
| Lust | — | ✅ |

## 🎨 獨立設計建議

### 1. 移除死代碼 `adjust_weights()`
`src/core/drive_engine.py` 行 337-355 的 `adjust_weights()` 從未被任何地方調用 (`grep -rn "adjust_weights(" src/` 返回空)。功能與 `normalize_weights()` 重疊。建議：
- 刪除 `adjust_weights()` 定義
- 統一使用 `normalize_weights()`

### 2. Veto Power 邏輯不一致 (ego_core.py 行 180-198)
第一層 loop 追蹤 `max_veto_power`，但第二層 `break` 只係用 first `>= 1.0` engine。兩者邏輯不一致：
```python
# 行 180-185：追蹤 max_veto_power
for engine in self.registry.get_all():
    veto_power = engine.get_veto_power()
    if veto_power > max_veto_power:
        max_veto_power = veto_power  # ← 計算了但從未使用

# 行 196-198：break 基於 first >= 1.0，唔係 max_veto_power
for engine in self.registry.get_all():
    if engine.get_veto_power() >= 1.0:
        engine.state.veto_used = True
        break  # ← max_veto_power engine 可能唔係 first >= 1.0
```
建議：第二層 loop 應基於 `max_veto_power` 而非 first >= 1.0。

## 📝 忽略清單
✅ Hermes/Sisyphus 重構沉澱期，無需干預

## 待關注 (Watch List)
1. `adjust_weights()` 死代碼 — 計畫移除
2. ego_core.py veto power 邏輯不一致 — 計畫修復

## 心情：😐
